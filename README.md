# KeepGoTestApp Mobile

## Setup

- Run `yarn` to install dependencies
- `cd ios && pod install`

## Usage

### IOS

- Open ios/Zoozy.xcworkspace file
- Run on device or simulator

### Android

- `yarn android` (see other scripts in package.json file)
